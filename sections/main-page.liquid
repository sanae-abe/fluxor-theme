<div class="px-5 py-16">
  <section class="max-w-3xl mx-auto">
    <h1 class="text-2xl md:text-4xl font-semibold mb-10">{{ page.title }}</h1>
    <div class="prose prose-sm max-w-none leading-relaxed text-stone-500">
      {{ page.content }}
    </div>
  </section>
</div>

{% schema %}
{
  "name": "Page"
}
{% endschema %}
