<section class="max-w-2xl mx-auto px-5 py-16">
  <h1 class="text-4xl font-semibold mb-10">{{ page.title }}</h1>
  <div class="prose prose-sm max-w-none leading-relaxed opacity-80">
    {{ page.content }}
  </div>
</section>

{% schema %}
{
  "name": "Page"
}
{% endschema %}
