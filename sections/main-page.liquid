{%- comment %} ページヒーロー {%- endcomment -%}
<section class="w-full px-4 md:px-10 py-10 md:py-16 border-b border-stone-200">
  <div class="max-w-3xl mx-auto">
    <h1 class="text-2xl md:text-3xl font-medium text-text">{{ page.title }}</h1>
  </div>
</section>

{%- comment %} ページコンテンツ {%- endcomment -%}
<section class="w-full px-4 md:px-10 py-10 md:py-[60px]">
  <div class="max-w-3xl mx-auto">
    <div class="page-content">
      {{ page.content }}
    </div>
  </div>
</section>

{%- schema %}
{
  "name": "Page",
  "presets": [
    {
      "name": "Page"
    }
  ]
}
{%- endschema -%}
